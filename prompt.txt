Update src/components/Student/QuizSelection.tsx:

1. Add state for recommendations: const [recommendations, setRecommendations] = useState([])
2. Fetch from /api/recommendations?limit=5 on component mount
3. Add "Recommended Quizzes" section at top (before game selection):
   - Show 3-5 recommended quizzes in cards
   - Each card shows: subject, chapter, reason, "Start Quiz" button
   - Style prominently (gradient background, larger cards)
4. When "Start Quiz" clicked:
   - Auto-populate selectedGame, selectedSubject, selectedChapter
   - Call startGame() automatically
   - Show toast: "Starting recommended quiz..."









   Update the question data structure:

1. Check src/utils/questionsData.ts - ensure each question has:
   - conceptTags: string[] (e.g., ["linear_equations", "algebra"])
   - difficultyLevel: number (0-1)
   - errorType: string (if wrong, what type: "calculation", "concept", "application", "careless")

2. If questions are in JSON files (public/games/Questions.json), add these fields:
   - Update the TypeScript interface/type for questions
   - Add conceptTags and difficultyLevel to sample questions

3. Create server/utils/questionMapper.js:
   - Function: extractConceptsFromQuestion(question) - returns conceptTags array
   - Function: getQuestionDifficulty(question) - returns difficultyLevel
   - Function: inferErrorType(question, studentAnswer, correctAnswer) - returns errorType












   Create server/services/feedbackEngine.js:

1. async generatePostQuizFeedback(studentId, quizResults)
   - quizResults: { quizId, answers: [{ questionId, isCorrect, conceptTags, timeSpent, errorType, studentAnswer, correctAnswer }] }
   - For each answer, update concept mastery using learnerModel.updateConceptMastery
   - Calculate concept breakdown:
     * Group answers by concept
     * Calculate mastery per concept: correctCount / totalCount
     * Identify strengths (mastery > 0.8) and weaknesses (mastery < 0.5)
   - Generate feedback messages:
     * Strengths: "Excellent work on [concept]!"
     * Weaknesses: "Let's practice more [concept]. Here's why..."
   - Generate recommendations: find lessons/quizzes targeting weak concepts
   - Return: {
       overallScore: number,
       conceptBreakdown: [{ concept, mastery, questions, correct, feedback }],
       strengths: [],
       weaknesses: [],
       recommendations: [{ type, concept, content }],
       personalizedMessage: string
     }

2. generateRealTimeHint(questionId, studentAnswer, attemptNumber)
   - Return progressive hints:
     * Attempt 1: "Think about the key concept here..."
     * Attempt 2: "Remember: [specific formula/concept]"
     * Attempt 3: "Step-by-step: [detailed guidance]"
   - Store hints in memory or database

3. explainConcept(conceptName, studentLevel)
   - studentLevel: 'beginner' | 'intermediate' | 'advanced' (based on mastery)
   - Return explanation tailored to level
   - Can use simple text or call external API for explanations














   In server/index.js, add:

1. POST /api/feedback/quiz-complete
   - Body: { quizId, answers: [{ questionId, isCorrect, conceptTags, timeSpent, errorType, studentAnswer, correctAnswer }] }
   - Requires authenticateToken
   - Calls feedbackEngine.generatePostQuizFeedback(req.user.id, req.body)
   - Also updates student_progress table
   - Returns feedback object

2. GET /api/feedback/hint?questionId=xxx&attemptNumber=1
   - Requires authenticateToken
   - Returns hint string

3. GET /api/feedback/explain?concept=fractions
   - Requires authenticateToken  
   - Returns explanation object: { concept, explanation, examples: [] }

Add: const feedbackEngine = require('./services/feedbackEngine');

















Create src/components/Student/FeedbackDisplay.tsx:

1. Props: { feedback: { overallScore, conceptBreakdown, strengths, weaknesses, recommendations, personalizedMessage } }
2. Display:
   - Header: Score with celebration animation (confetti if > 80%)
   - Concept Breakdown: Progress bars for each concept with mastery %
   - Strengths Section: Green badges for mastered concepts
   - Weaknesses Section: Red/orange cards with "Practice" buttons
   - Recommendations: Clickable cards linking to recommended content
   - Personalized Message: Large text with encouragement
3. Style with Tailwind: gradients, animations, icons from lucide-react
4. Export component
















Find where quizzes complete (GamePlayer.tsx or quiz game files):

1. After quiz completion, collect all answers with conceptTags and errorType
2. Call POST /api/feedback/quiz-complete with quiz results
3. Show FeedbackDisplay component with the returned feedback
4. Add navigation: "Continue Learning" button that goes to recommendations
5. Store feedback in state and show in modal or new page













Create server/services/analyticsService.js:

1. async getClassAnalytics(teacherId)
   - Get all students in teacher's school/class
   - Query student_progress, concept_mastery, student_learning_profile
   - Calculate:
     * totalStudents: COUNT(DISTINCT students)
     * activeToday: COUNT(students with activity today)
     * averageCompletion: AVG(lessons_completed)
     * averageScore: AVG(scores)
     * atRiskStudents: students with engagement_score < 0.4 OR mastery < 0.3
     * topPerformers: students with mastery > 0.8
     * strugglingStudents: students with mastery < 0.5
     * subjectPerformance: { Mathematics: avg_score, Science: avg_score, ... }
     * weeklyEngagement: [{ day: "Monday", engagement: 0.8 }, ...]
   - Return comprehensive analytics object

2. async getStudentDetailedAnalytics(teacherId, studentId)
   - Verify student belongs to teacher's class
   - Get detailed data:
     * Mastery by concept (heatmap data)
     * Learning velocity trends (line chart data)
     * Engagement trends (daily activity)
     * Recommended interventions (based on weak concepts)
   - Return detailed analytics object

3. async getPerformanceDistribution(teacherId)
   - Group students by performance ranges: 90-100%, 80-89%, 70-79%, 60-69%, <60%
   - Return: [{ range: "90-100%", count: 5 }, ...]











   In server/index.js:

1. Update GET /api/analytics/:teacherId endpoint:
   - Replace simple query with analyticsService.getClassAnalytics(teacherId)
   - Return comprehensive analytics

2. Add GET /api/analytics/student/:studentId
   - Requires authenticateToken, requireRole('teacher')
   - Verify student is in teacher's class
   - Call analyticsService.getStudentDetailedAnalytics(req.user.id, studentId)
   - Return detailed analytics

3. Add GET /api/analytics/performance-distribution
   - Requires authenticateToken, requireRole('teacher')
   - Call analyticsService.getPerformanceDistribution(req.user.id)
   - Return distribution array

Add: const analyticsService = require('./services/analyticsService');


















Update src/components/Teacher/Dashboard.tsx:

1. Replace all hardcoded zeros with real data:
   - Add useState for analytics data
   - Add useEffect to fetch from /api/analytics/:teacherId on mount
   - Show loading state
   - Handle errors

2. Add "At-Risk Students" section:
   - List students with risk indicators
   - Show risk score, reasons (low engagement, low mastery)
   - "View Details" button for each student

3. Update "Weekly Engagement" chart:
   - Use recharts LineChart or BarChart
   - Use real weeklyEngagement data from API

4. Update "Performance Distribution":
   - Use real data from /api/analytics/performance-distribution
   - Show bar chart with actual counts

5. Add "Recent Activity" section:
   - Query recent student_progress entries
   - Show: student name, activity, score, time
   - Update in real-time or on refresh



















   Create src/components/Teacher/StudentDetailView.tsx:

1. Props: { studentId: string, onClose?: () => void }
2. Fetch from /api/analytics/student/:studentId on mount
3. Display:
   - Student Profile Card: name, class, photo, overall stats
   - Mastery Heatmap: Grid of concepts with color coding (green=mastered, yellow=learning, red=weak)
   - Learning Velocity Chart: Line chart showing questions/min over time
   - Engagement Trends: Line chart showing daily engagement
   - Recommended Interventions: List of AI-suggested actions
   - Action Buttons: "Send Message", "Assign Remedial Work", "Schedule Meeting"
4. Use recharts for visualizations
5. Style with Tailwind
6. Add routing in App.tsx to show this component















Create server/services/pacingEngine.js:

1. async calculateOptimalPacing(studentId, subject)
   - Get student_learning_profile for student
   - Calculate:
     * questionsPerSession: Based on average_learning_velocity and engagement_score
     * breakFrequency: Based on average_session_duration (suggest break every 20-30 min)
     * sessionDuration: Optimal session length based on engagement patterns
     * dailyGoal: Recommended questions per day
   - Return pacing object

2. async shouldTakeBreak(studentId, currentSessionTime)
   - Check engagement_patterns for optimal break times
   - Return true if sessionTime > optimal duration

3. async adjustContentPace(studentId, recentPerformance)
   - Analyze last 10 quiz results
   - If performance improving: increase questionsPerSession
   - If performance declining: decrease questionsPerSession, suggest breaks
   - Update student_learning_profile

Add API: GET /api/pacing/optimal?subject=X
















Update quiz/game question selection logic:

1. Before quiz starts:
   - Call GET /api/learner/recommended-difficulty?subject=X
   - Filter questions by difficulty matching student's optimal difficulty ± 0.2

2. During quiz (dynamic adjustment):
   - Track consecutive correct/incorrect answers
   - If 3 correct in a row: increase difficulty by 0.1
   - If 2 wrong in a row: decrease difficulty by 0.1
   - Update question selection accordingly

3. Find GamePlayer.tsx or quiz game files and update question selection logic
4. Add difficulty indicator to UI: "Difficulty: Easy/Medium/Hard"
























Update server/services/learnerModel.js:

1. Add function detectLearningStyle(studentId):
   - Query student_learning_profile for time_spent_visual, time_spent_reading, time_spent_practice
   - Calculate engagement_by_modality percentages
   - Determine style:
     * visual: if visual > 60% of total time
     * reading: if reading > 60% of total time  
     * kinesthetic: if practice > 60% of total time
     * mixed: if no single modality > 60%
   - Update preferred_learning_style in profile
   - Return detected style

2. Call detectLearningStyle after each learning session
3. Add API: GET /api/learner/learning-style























Update server/services/recommendationEngine.js:

1. Modify getRecommendedContent to consider learning style:
   - Get preferred_learning_style from student_learning_profile
   - Filter content by modality:
     * visual learners: prefer games, videos, interactive content
     * reading learners: prefer text lessons, articles
     * kinesthetic: prefer practice quizzes, hands-on
     * mixed: balanced mix
   - Boost priority_score for matching modality

2. Update frontend Lessons.tsx:
   - Show content in preferred format first
   - Add badge: "Recommended for your learning style"


















   Create server/services/adminAnalyticsService.js:

1. async getPlatformAnalytics()
   - Query all users, student_progress, concept_mastery
   - Calculate:
     * totalUsers, activeUsers, completionRates
     * schoolPerformanceComparison: [{ schoolName, avgScore, completionRate }]
     * subjectDifficultyAnalysis: [{ subject, avgScore, avgTime, difficultyScore }]
     * topStrugglingConcepts: concepts with lowest average mastery
     * engagementTrends: platform-wide engagement over time
   - Generate AI insights:
     * "Top 3 struggling concepts: [list]"
     * "Recommended content updates: [suggestions]"
     * "Engagement trends: [analysis]"
   - Return comprehensive platform analytics

2. Add API: GET /api/admin/analytics
   - Requires authenticateToken, requireRole('admin')
   - Returns platform analytics

















   Update src/components/Admin/AdminAnalytics.tsx:

1. Replace placeholder with real dashboard:
   - Fetch from /api/admin/analytics on mount
   - Platform Stats Cards: total users, active users, completion rates
   - School Performance Comparison: Table or bar chart
   - Subject Difficulty Analysis: Heatmap or bar chart
   - AI Insights Section:
     * Top struggling concepts (with links to content)
     * Recommended content updates
     * Engagement trends (line chart)
   - Use recharts for visualizations
   - Style with Tailwind
   - Show loading and error states


















   Create server/services/experimentation.js:

1. Track recommendation strategies:
   - "mastery_based": Focus on weak concepts
   - "sequence_based": Follow curriculum order
   - "engagement_based": Prioritize engaging content

2. Randomly assign students to strategies (50/50 split)
3. Track outcomes: learning gains, engagement, completion
4. Store in experiments table

5. Add API: GET /api/experiments/strategy?studentId=X
   - Returns current strategy for student

Quick implementation: Just assign strategies randomly and log results.




















Update quiz completion flow to automatically update learner model:

1. When quiz completes, extract conceptTags from each question
2. For each answer:
   - Call learnerModel.updateConceptMastery(studentId, conceptName, isCorrect, timeSpent)
   - Track errorType if wrong
   - Update error_patterns table
   - Update engagement_patterns table with session data

3. Update student_learning_profile:
   - Recalculate learning_velocity
   - Update engagement_score
   - Detect learning style

4. This should happen automatically in the quiz completion endpoint

















Add consistent loading states and error handling across all new components:

1. Create src/components/common/LoadingSpinner.tsx
2. Create src/components/common/ErrorDisplay.tsx
3. Update all new components to use these
4. Add error boundaries where needed
5. Show user-friendly error messages















Add real-time or periodic updates:

1. Use setInterval in Dashboard components to refresh data every 30 seconds
2. Or use WebSocket/Server-Sent Events for real-time updates (optional)
3. Add "Refresh" buttons to manual updates
4. Show "Last updated: [time]" indicators